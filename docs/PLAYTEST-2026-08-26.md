# Playtest notes — 2026-08-26 (user, Pittsburgh save)

Third regroup, and the largest. 25 findings. The centre of gravity has moved:
the systems are mostly there now, and what's wrong is **how the game reads,
sounds and feels**. Two themes dominate — **prose quality/logic** and
**TTS performance** — and both are cross-cutting rather than one screen's bug.

Grouped by theme. `bar` ties to docs/EXCELLENCE.md.

---

## A. WRITING — the dominant theme

The user: *"Maybe we need a thorough dedicated agent just for all of the writing
of things in the game and also to make them more unique and not repetitive.
Priority being logic though."*

| # | Finding | Bar |
|---|---------|-----|
| A1 | **Dynamic player/staff biographies (NEW FEATURE).** FM-style prose telling the story of a career, updating over time, immersive — *"I dont want it to feel like it was written by AI"*. Must be **pre-populated for existing players** who already have a career behind them. Placement TBD: History tab vs Information tab — decide deliberately. | B5.1 |
| A2 | **Season review fires before the season exists.** At game start: *"2025–2026 season review: Pittsburgh Penguins over-delivered on every expectation… finish at 0–0–0 (0 pts, 15th of 32)"*. Nonsense. **Logic before flourish.** | B5.2 |
| A3 | **Long messages are cut off with no way to read the rest.** The 2027 draft-class breakdown truncates mid-list. Needs scrolling. | B7.3 |
| A4 | **"the board" is used constantly** — for almost everything, in almost every scouting string. Vocabulary needs range. | B5.1 |
| A5 | **Postgame write-up wanted.** The receipt is liked; add a short match story with *flavour* — a wacky goal, a low-percentage event surfacing rarely. Keep **"The Turning Point"** as its own one-liner. Decide whether the write-up is separate to avoid clutter. Treat "adding flavour" as a general goal, not one feature. | B6.2 |
| A6 | **Player tweets are too corny** — emoji-heavy. Possibly funny, possibly grating; needs a judgement call and a tone pass. | B5.1 |
| A7 | **BREAKING tag needs curation** — applied too loosely to mean anything. | B4.5 |
| A8 | **Inbox is cluttered** with low-value items ("expiring deal adds intrigue", players "closing in on" milestones). Must be *masterfully curated by value and interest*. | B2.1 |

---

## B. TTS & PERFORMANCE — the other dominant theme

| # | Finding | Severity |
|---|---------|----------|
| B1 | **Every TTS event freezes the game**, and the line takes a long time to arrive. *"We need to think of a better solution."* Not a tuning problem — the approach needs rethinking. | critical |
| B2 | **TTS speaks about the player in the third person.** "Blake Lizotte — *'Lizotte appreciated being heard, even if nothing was promised.'*" Either don't speak it, or make it real dialogue. | major |
| B3 | **Scout meeting: voices spam**, and everything is dumped on one screen. Staff should raise items **one at a time**. | major |
| B4 | **Windows security blocks TTS.** Related to the code-signing item the user has deferred — record, don't chase. | blocked |
| B5 | **Postgame screen is slow to load and clunky** — has to click away or hit space to move on. Cadence is otherwise much improved. | major |

---

## C. SCOUTING — full UI overhaul

| # | Finding | Bar |
|---|---------|-----|
| C1 | **Watch List starts pre-populated with star players.** Should be **empty until the user fills it**. | B7.3 |
| C2 | **Scouting Centre says almost nothing** — one line about being thin at a position. Needs real insight. | B7.3 |
| C3 | **Players tab is pre-filled with A+ targets ranked by value.** Proposal: make it the **whole-database player search** with custom filters (age, attributes, position, contract…) — a real scouting tool. | B7.3 |
| C4 | **Scout coverage map is "ugly blobs."** Wants a genuinely beautiful globe worth looking at. | B7.3 |
| C5 | **Prospect rankings pre-populated** — finding high-potential prospects is far too easy and unrealistic. Scouting should be *work*. | B3.2 |

**Resolved 2026-08-27 — all five.** The through-line was that the game answered
the scouting question for you on turn one; the fix is that it now asks it.

- **C1** — `ScoutingState.watchList` is the GM's own list. It starts empty and
  only grows by an explicit act (right-click menu, profile ☆, the search's star,
  or "★ Track him" in the Centre, which is now the same act). It is not a
  bookmark: `tickScouting` gives pinned players the front of every scout's day —
  at full read rate, capped at half his capacity — whatever his brief says, and
  shields their files from decay. Capped at 40, so a pin costs regional coverage.
- **C2** — `scoutBriefing.ts` builds a real department briefing: bandwidth and
  strain, draft-class coverage, the beats you cover, the youth pools nobody is
  watching, the prospects your own scouts rank 8+ spots apart, and what actually
  moved since the last month-start snapshot (`ScoutingState.coverageLog`).
- **C3** — the Players tab is `playerSearch.ts`: the whole database, filtered by
  name / position / age / league / nation / handedness / contract / cap hit /
  production / read quality, sorted on any column. Ability and ceiling are the
  department's read and come back `null` for anyone unscouted — the search cannot
  see through the fog.
- **C4** — `ScoutGlobe.tsx`: an orthographic canvas globe (dot-field landmasses,
  graticule, atmosphere, coverage arcs from your club), lit nodes where you have
  eyes and dashed rings on the blind spots, paired with a clickable beat list that
  spins the world to a country.
- **C5** — the U17 radar is gated on a real read (it used to hand you the next two
  classes sorted by ceiling); your own board shows "no read" instead of a star
  grade for prospects nobody has watched, and states what fraction of the class
  you have filed on; the public board publishes a rank and a projected ROLE, not a
  ceiling grade. Root cause found and fixed: `createInitialScouting` only fogged
  draft-*eligible* amateurs, so every 14–16-year-old in the world started at
  renown knowledge — which is why the radar arrived pre-filled.

---

## D. TRADE & LIST SCREENS

| # | Finding | Bar |
|---|---------|-----|
| D1 | **Counter doesn't pre-select the offered assets.** Countering should start from what was proposed. | B7.3 |
| D2 | **Hovering the value bar exposes raw numbers and the reasoning.** *"That information shouldn't be visible to the player."* The bar was the point. | B7.3 |
| D3 | **Trade block is one long list** — needs a modern, browsable overhaul. | B7.3 |
| D4 | **Staff tab is one giant list** — same problem. | B7.3 |
| D5 | **Sorting is missing across many pages.** *"I find myself trying to sort and arrange but its lacking on most of them."* | B7.3 |

---

## E. SYSTEMS & DEPTH

| # | Finding | Bar |
|---|---------|-----|
| E1 | **GM-of-the-whole-club roleplay.** Dev-centre and prospect life should generate real beats: post-draft phone calls, offseason meetings, a role/needs conversation with a newly acquired player (and that should be part of negotiation *beforehand* too). Prospect seasons should surface in meetings/inbox through the year — **without spam**. Reference: Kyle Dubas attending AHL playoffs. *"the game is about the entire club system not just the NHL players."* | B5.5 |
| E2 | **The early-extension event does nothing.** "Chinakhov is waiting in your office… sign the extension NOW, a year early, below what he'll be worth" — but mid-season extensions aren't possible. The event promises an action the game won't honour. | B2.4 |
| E3 | **No pressure from fans or ownership.** Coaches should be fireable around the league, and **the user should be fireable** for sustained underperformance, scaled to expectations and within realistic bounds. | B4.4 |

---

## F. UI POLISH

| # | Finding | Bar |
|---|---------|-----|
| F1 | **Snowflake icon for general emails** — disliked. | B7.3 |
| F2 | **"What this board is worth ≈ 4.3 pts on the table" is visible on Tactics.** Almost certainly internal lever-audit output that escaped into the UI. | B7.3 |
| F3 | **Star-rating colours (gold/green/grey) are unexplained** and not intuitive. | B7.3 |
| F4 | **Sim view needs polish** — things jump around; wants a professional broadcast-style dashboard. | B6.1 |
| F5 | **The Feed should look like social media**, not a list: verified badges colour-coded by author type (player/journalist/club), the ability to **follow any account** rather than only those in the top strip, and a followed-feed view. More flavour. | B5.3 |
| F6 | **The meta game needs to feel like a game, not a dev demo** — save/load, new game, team pick, and everything outside the normal screens. | B1.1 |

---

## NOT A FINDING

The note *"Work out a way to inpaint and upscale the distorted parts of gpt2 at
smaller pixel levels or for people"* appears to have been pasted from another
project. No action taken; flagged in case it was meant elsewhere.

---

## Reading of this playtest

The engine work has landed — trades, re-signing, gates, retirement, levers. What
the user is now reacting to is almost entirely **presentation and prose**: text
that is illogical, repetitive or truncated; a voice layer that freezes the game;
lists where there should be tools. That is a good problem to have, and it says
the next phase is a **craft** phase rather than a systems phase.

The one item that is neither: **E3 (pressure and firing)**, which is a real
missing system and the thing that would give every other number stakes.
